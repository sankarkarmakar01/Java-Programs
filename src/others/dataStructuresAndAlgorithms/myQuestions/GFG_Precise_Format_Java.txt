Link:- https://www.geeksforgeeks.org/problems/precise-fomat-java/1?page=1&category=Java&sortBy=submissions

class Solution {
    // Function to return an ArrayList with exact result and formatted result
    static ArrayList<Float> divisionWithPrecision(float a, float b) {
        // code here
        ArrayList<Float> ans = new ArrayList<>();

        float first = a/b;
        float second = Math.round(first * 1000f) / 1000f;

        ans.add(first);
        ans.add(second);

        return ans;

    }
}